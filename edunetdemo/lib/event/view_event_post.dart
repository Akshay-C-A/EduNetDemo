import 'package:flutter/material.dart';

class ViewEventPost extends StatelessWidget {
  const ViewEventPost({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}